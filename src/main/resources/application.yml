spring:
  web:
    resources:
      static-locations: file:uploads/
  profiles:
    active: dev
  application:
    name: certificate-hub

server:
  servlet:
    context-path: /certification-hub/api

  multipart:
    enabled: true
    max-file-size: 5MB
    max-request-size: 5MB
  port: 8080



